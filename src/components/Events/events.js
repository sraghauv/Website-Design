/* 
    This exports all the components for the Events
    page from each of their JSX files inside the 
    Events folder, so it's easier to import multiple 
    components needed for the Events page
    from this file!
*/

import Event from "./Event";
import EventsDisplay from "./EventsDisplay";
import Filter from "./Filter";

export { Event, EventsDisplay, Filter };